// "Au" is gold on the periodic table — a small nod worth calling out in the
// name itself. Rendered as a span so callers can drop it into an <h1>, a
// mono eyebrow, or the game header brand name and still get one @font-face.
export default function Wordmark() {
  return (
    <>
      <span className="wordmark-au">Au</span>thropeum
    </>
  );
}
